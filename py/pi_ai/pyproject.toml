[project]
name = "pi-ai"
version = "0.1.0"
description = "Unified multi-provider LLM API"
requires-python = ">=3.12"
license = "MIT"
dependencies = [
    "google-genai>=1.0",
    "boto3>=1.35",
    "httpx>=0.27",
]

[tool.setuptools.packages.find]
where = [".."]
include = ["pi_ai*"]
