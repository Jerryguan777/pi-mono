[project]
name = "pi-ai"
version = "0.1.0"
description = "Unified multi-provider LLM API"
requires-python = ">=3.12"
license = "MIT"
dependencies = [
    "partial-json-parser>=0.2",
    "anthropic>=0.49",
    "openai>=1.60",
    "httpx>=0.27",
    "websockets>=14",
    "google-genai>=1.0",
    "boto3>=1.35",
]
