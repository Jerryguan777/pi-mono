[project]
name = "pi-tui"
version = "0.1.0"
description = "Terminal UI with differential rendering"
requires-python = ">=3.12"
license = "MIT"
dependencies = [
    "wcwidth>=0.2",
    "mistune>=3.0",
]
