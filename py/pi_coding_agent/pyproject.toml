[project]
name = "pi-coding-agent"
version = "0.1.0"
description = "Interactive coding agent CLI"
requires-python = ">=3.12"
license = "MIT"
dependencies = [
    "pi-ai",
    "pi-agent",
    "filelock>=3.0",
    "pyyaml>=6.0",
]
